# Noosphera Database Setup

PostgreSQL 18 otimizado para IA com pgvector, JSONB e integração LangChain/Neo4j.

## Pré-requisitos
- Docker e Docker Compose
- Make (opcional)

## Instalação

1. Copie `.env.example` para `.env` e ajuste as variáveis:
   ```bash
   cp .env.example .env
